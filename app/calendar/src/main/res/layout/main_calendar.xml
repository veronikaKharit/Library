<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="53dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"

            android:background="@drawable/rounded_corner_month">

            <!-- Кнопка влево для предыдущего месяца -->
            <ImageButton
                android:id="@+id/imageButton1"
                android:layout_height="38dp"
                android:layout_width="40dp"
                android:contentDescription="Предыдущий месяц"
                android:src="@drawable/ic_launcher_foreground2" />

            <!-- Название месяца -->
            <TextView
                android:id="@+id/MonthName"
                android:layout_height="42dp"
                android:fontFamily="@font/urchem_sans_regular"
                android:text="Май, 2025"

                android:layout_width="220dp"
                android:textAlignment="center"
                android:textColor="#000000"
                android:textSize="22sp"
                android:textStyle="bold" />

            <!-- Кнопка вправо для следующего месяца -->
            <ImageButton
                android:id="@+id/imageButton2"
                android:layout_width="40dp"
                android:layout_height="38dp"
                android:contentDescription="Следующий месяц"
                android:src="@drawable/ic_launcher_foreground45" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>

    <LinearLayout
        android:id="@+id/eventContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"></LinearLayout>


</LinearLayout>
